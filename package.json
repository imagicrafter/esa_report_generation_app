{
  "name": "westx-prototype",
  "version": "1.0.0",
  "description": "Phase 2 ESA Report Generation Prototype",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:clean": "./scripts/dev.sh",
    "stop": "lsof -ti:3001 | xargs kill -9 2>/dev/null || echo 'No processes found on port 3001'",
    "restart": "npm run stop && sleep 1 && npm run dev",
    "build": "echo 'No build step required'",
    "test": "echo 'No tests specified'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}